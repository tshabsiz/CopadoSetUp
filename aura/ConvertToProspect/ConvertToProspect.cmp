<aura:component controller="MatrixLeadConversion" implements="force:lightningQuickAction,force:hasRecordId"  >
        <aura:attribute name="record" type="Matrix_Lead__c"/>
    
    	<force:recordData aura:id="recordLoader"
                      recordId="{!v.recordId}"
                      layoutType="FULL"
                      targetRecord="{!v.record}"
                      targetFields="{!v.simpleRecord}"
                      recordUpdated="{!c.doInit}"
                      />
</aura:component>